[package]
name = "bloc_fantome_native"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[profile.release]
lto = "thin"
codegen-units = 1
panic = "abort"
strip = "symbols"
